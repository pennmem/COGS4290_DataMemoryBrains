{
  "module": 21,
  "assignment": 10,
  "title": "Representational Similarity Analysis",
  "student_notebook": "Module_21_RSA.ipynb",
  "solution_notebook": "Solution_Module_21.ipynb",
  "answers": [
    {
      "key": "Q1c_within_list_serialpos_rsa_mat",
      "question": "1c",
      "role": "plot",
      "type": "array",
      "shape_hint": "(11, 11)",
      "student_hint": "within-list serial-position RSA heatmap: list-centered z-transformed correlations, earlier item (rows) x later item (cols); save the 2-D array under the heatmap, not the image",
      "solution_expr": "within_list_WORD_rsa_serialpos_mat.values"
    },
    {
      "key": "Q1e_serialpos_dist_rsa",
      "question": "1e",
      "role": "plot",
      "type": "dataframe",
      "columns": [
        "subject_WORD",
        "serialpos_dist",
        "corr_z_list_adj"
      ],
      "student_hint": "per-subject mean list-centered z-correlation as a function of study distance (serialpos_dist) — the Manning-plot point heights",
      "solution_expr": "within_list_WORD_rsa_df.groupby(['subject_WORD','session_WORD','list_WORD','serialpos_dist'],as_index=False).agg({'corr_z_list_adj':'mean'}).groupby(['subject_WORD','session_WORD','serialpos_dist'],as_index=False).agg({'corr_z_list_adj':'mean'}).groupby(['subject_WORD','serialpos_dist'],as_index=False).agg({'corr_z_list_adj':'mean'})"
    },
    {
      "key": "Q1f_serialpos_dist_category_rsa",
      "question": "1f",
      "role": "plot",
      "type": "dataframe",
      "columns": [
        "subject_WORD",
        "serialpos_dist",
        "same_category",
        "corr_z_list_adj"
      ],
      "student_hint": "per-subject mean list-centered z-correlation by study distance (serialpos_dist) and same_category (hue) — the point-plot heights",
      "solution_expr": "within_list_WORD_rsa_subject_df"
    },
    {
      "key": "Q2c_serialpos_lag_rsa",
      "question": "2c",
      "role": "plot",
      "type": "dataframe",
      "columns": [
        "subject_WORD",
        "serialpos_lag",
        "corr_z_outpos_adj"
      ],
      "student_hint": "per-subject retrieval-centered z-correlation by serialpos_lag, restricted to lags -5..5 — the plotted point heights",
      "solution_expr": "within_list_correct_REC_WORD_rsa_subject_df.query('serialpos_lag <= 5 and serialpos_lag >= -5')"
    },
    {
      "key": "Q2d_serialpos_lag_category_rsa",
      "question": "2d",
      "role": "plot",
      "type": "dataframe",
      "columns": [
        "subject_WORD",
        "same_category",
        "serialpos_lag",
        "corr_z_outpos_adj"
      ],
      "student_hint": "per-subject retrieval-centered z-correlation by serialpos_lag and same_category (hue), lags -5..5 — the plotted point heights",
      "solution_expr": "within_list_REC_WORD_rsa_subject_df.query('serialpos_lag <= 5 and serialpos_lag >= -5')"
    },
    {
      "key": "Q3_study_dist_recall_category_rsa",
      "question": "3",
      "role": "plot",
      "type": "dataframe",
      "columns": [
        "subject_WORD",
        "serialpos_dist",
        "same_category",
        "pair_recalled",
        "corr_z_list_adj"
      ],
      "student_hint": "per-subject mean list-centered z-correlation by study distance (serialpos_dist), same_category (col facet), and pair_recalled (hue) — the point-plot heights",
      "solution_expr": "within_list_WORD_rsa_subject_df"
    }
  ]
}
